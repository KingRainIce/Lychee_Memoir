# image = 彩色平面图（默认） | geo = MapLibre 街图
VITE_BASEMAP_MODE=image

# 平面图须放在 public/ 下：支持 jpg/png/webp，或带透明底的 SVG
VITE_BASEMAP_IMAGE=/campus-basemap.jpg

# 后端 API 由 Vite 开发服务器代理到 http://localhost:8000（见 vite.config.ts；Windows+Docker 勿用 127.0.0.1）
# 生产环境请把前端与 API 同源部署，或自行配置反向代理。
